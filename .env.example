# Copy to .env and fill in (optional: export in shell instead; .env is gitignored).
# Also copy to green_agent/.env for ADK agent use.
GOOGLE_API_KEY=your-gemini-api-key
EARTH_ENGINE_PROJECT=your-gcp-project-id
GOOGLE_SOLAR_API_KEY=your-solar-api-key
